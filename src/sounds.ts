export const ac = new AudioContext();export const boom = ac.decodeAudioData((window as unknown as {sounds: {boom: Uint8Array}}).sounds.boom.buffer)
export const bossboom1 = ac.decodeAudioData((window as unknown as {sounds: {bossboom1: Uint8Array}}).sounds.bossboom1.buffer)
export const bossboom2 = ac.decodeAudioData((window as unknown as {sounds: {bossboom2: Uint8Array}}).sounds.bossboom2.buffer)
export const b_fire = ac.decodeAudioData((window as unknown as {sounds: {b_fire: Uint8Array}}).sounds.b_fire.buffer)
export const cancel = ac.decodeAudioData((window as unknown as {sounds: {cancel: Uint8Array}}).sounds.cancel.buffer)
export const disrupt = ac.decodeAudioData((window as unknown as {sounds: {disrupt: Uint8Array}}).sounds.disrupt.buffer)
export const furybegin = ac.decodeAudioData((window as unknown as {sounds: {furybegin: Uint8Array}}).sounds.furybegin.buffer)
export const furyend = ac.decodeAudioData((window as unknown as {sounds: {furyend: Uint8Array}}).sounds.furyend.buffer)
export const graze = ac.decodeAudioData((window as unknown as {sounds: {graze: Uint8Array}}).sounds.graze.buffer)
export const l_fire = ac.decodeAudioData((window as unknown as {sounds: {l_fire: Uint8Array}}).sounds.l_fire.buffer)
export const m1b1 = ac.decodeAudioData((window as unknown as {sounds: {m1b1: Uint8Array}}).sounds.m1b1.buffer)
export const m1b2 = ac.decodeAudioData((window as unknown as {sounds: {m1b2: Uint8Array}}).sounds.m1b2.buffer)
export const m1i = ac.decodeAudioData((window as unknown as {sounds: {m1i: Uint8Array}}).sounds.m1i.buffer)
export const m2b1 = ac.decodeAudioData((window as unknown as {sounds: {m2b1: Uint8Array}}).sounds.m2b1.buffer)
export const m2b2 = ac.decodeAudioData((window as unknown as {sounds: {m2b2: Uint8Array}}).sounds.m2b2.buffer)
export const m2b3 = ac.decodeAudioData((window as unknown as {sounds: {m2b3: Uint8Array}}).sounds.m2b3.buffer)
export const m2b4 = ac.decodeAudioData((window as unknown as {sounds: {m2b4: Uint8Array}}).sounds.m2b4.buffer)
export const m2i = ac.decodeAudioData((window as unknown as {sounds: {m2i: Uint8Array}}).sounds.m2i.buffer)
export const m3b1 = ac.decodeAudioData((window as unknown as {sounds: {m3b1: Uint8Array}}).sounds.m3b1.buffer)
export const m3b2 = ac.decodeAudioData((window as unknown as {sounds: {m3b2: Uint8Array}}).sounds.m3b2.buffer)
export const m3b3 = ac.decodeAudioData((window as unknown as {sounds: {m3b3: Uint8Array}}).sounds.m3b3.buffer)
export const m3b4 = ac.decodeAudioData((window as unknown as {sounds: {m3b4: Uint8Array}}).sounds.m3b4.buffer)
export const m3b5 = ac.decodeAudioData((window as unknown as {sounds: {m3b5: Uint8Array}}).sounds.m3b5.buffer)
export const m3i = ac.decodeAudioData((window as unknown as {sounds: {m3i: Uint8Array}}).sounds.m3i.buffer)
export const m4b = ac.decodeAudioData((window as unknown as {sounds: {m4b: Uint8Array}}).sounds.m4b.buffer)
export const m4i = ac.decodeAudioData((window as unknown as {sounds: {m4i: Uint8Array}}).sounds.m4i.buffer)
export const n_fire = ac.decodeAudioData((window as unknown as {sounds: {n_fire: Uint8Array}}).sounds.n_fire.buffer)
export const pickup = ac.decodeAudioData((window as unknown as {sounds: {pickup: Uint8Array}}).sounds.pickup.buffer)
export const r_fire = ac.decodeAudioData((window as unknown as {sounds: {r_fire: Uint8Array}}).sounds.r_fire.buffer)
export const select = ac.decodeAudioData((window as unknown as {sounds: {select: Uint8Array}}).sounds.select.buffer)
export const shield = ac.decodeAudioData((window as unknown as {sounds: {shield: Uint8Array}}).sounds.shield.buffer)
export const statspop = ac.decodeAudioData((window as unknown as {sounds: {statspop: Uint8Array}}).sounds.statspop.buffer)
export const toggle = ac.decodeAudioData((window as unknown as {sounds: {toggle: Uint8Array}}).sounds.toggle.buffer)
export const vblade = ac.decodeAudioData((window as unknown as {sounds: {vblade: Uint8Array}}).sounds.vblade.buffer)
export const vfirerate = ac.decodeAudioData((window as unknown as {sounds: {vfirerate: Uint8Array}}).sounds.vfirerate.buffer)
export const vfury = ac.decodeAudioData((window as unknown as {sounds: {vfury: Uint8Array}}).sounds.vfury.buffer)
export const vlaser = ac.decodeAudioData((window as unknown as {sounds: {vlaser: Uint8Array}}).sounds.vlaser.buffer)
export const vmultiplier = ac.decodeAudioData((window as unknown as {sounds: {vmultiplier: Uint8Array}}).sounds.vmultiplier.buffer)
export const vpeacemaker = ac.decodeAudioData((window as unknown as {sounds: {vpeacemaker: Uint8Array}}).sounds.vpeacemaker.buffer)
export const vrockets = ac.decodeAudioData((window as unknown as {sounds: {vrockets: Uint8Array}}).sounds.vrockets.buffer)
export const vshield = ac.decodeAudioData((window as unknown as {sounds: {vshield: Uint8Array}}).sounds.vshield.buffer)
export const warning = ac.decodeAudioData((window as unknown as {sounds: {warning: Uint8Array}}).sounds.warning.buffer)
